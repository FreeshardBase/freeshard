path_root: "run/"

log:
  levels:
    portal_core: debug
    'portal_core.service.app_usage_reporting': error

apps:
  usage_reporting:
    tracking_schedule: '* * * * * */30'
    reporting_schedule: '* * * * * 0'

management:
  api_url: http://localhost:8090
