tests:
  cache_ttl: 0

services:
  postgres:
    host: localhost
    port: 5432
    user: postgres
    password: postgres

apps:
  lifecycle:
    refresh_interval: 2

log:
  levels:
    'portal_core.service.app_lifecycle': debug
