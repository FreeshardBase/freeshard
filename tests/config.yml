services:
  postgres:
    host: localhost
    port: 5432
    user: postgres
    password: postgres

apps:
  initial_apps:
    filebrowser: null
    app-foo:
      image: bar
      port: 80
      data_dirs:
        - /data
        - /config
      authentication:
        default_access: private