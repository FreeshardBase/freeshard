services:
  postgres:
    connection_string: 'postgresql://postgres:postgres@localhost:5432'

apps:
  initial_apps:
    filebrowser: null
    app-foo:
      image: bar
      port: 80
      data_dirs:
        - /data
        - /config
      authentication:
        default_access: private