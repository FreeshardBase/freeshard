networks:
    portal:
        external: true

services:
    immich:
        restart: always
        image: nginx:alpine
        container_name: immich
        ports:
        - 80:80
        volumes:
        - "{{ fs.app_data }}:/data"
        - "{{ fs.shared }}:/data/shared"
        - "/var/run/docker.sock:/var/run/docker.sock:ro"
        networks:
        - portal
