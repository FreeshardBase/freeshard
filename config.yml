database:
  filename: /core/portal_core_db.json

user_data_dir: /user_data

dns:
  zone: p.getportal.org
  prefix length: 6

app_infra:
  compose_filename: /core/docker-compose-apps.yml
  traefik_dyn_filename: /core/traefik_dyn/traefik_dyn.yml

services:
  postgres:
    host: postgres
    port: 5432
    user: postgres
    password: ES9BOPusdVKoydq6

apps:
  app_store:
    project_id: 25524454
    sync_dir: /core/appstore
  lifecycle:
    refresh_interval: 10
  initial_apps:
    - filebrowser

log:
  portal_core: debug
