path:
  core: core
  user_data: user_data

dns:
  zone: p.getportal.org
  prefix length: 6

services:
  postgres:
    host: postgres
    port: 5432
    user: postgres
    password: ES9BOPusdVKoydq6
  backup:
    included_dirs:
      - /core
      - /user_data

apps:
  app_store:
    project_id: 25524454
  lifecycle:
    refresh_interval: 10
  initial_apps:
    - filebrowser

management:
  # api_url: foo

log:
  levels:
    portal_core: info
    gunicorn: warning
