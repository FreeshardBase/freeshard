database:
  filename: /core/portal_core_db.json

dns:
  zone: p.getportal.org
  prefix length: 6

docker_compose:
  compose_filename: /core/docker-compose-apps.yml

services:
  postgres:
    host: postgres
    port: 5432
    user: postgres
    password: ES9BOPusdVKoydq6

apps:
  app_data_dir: /app_data
  app_store:
    project_id: 25524454
    sync_dir: /core/appstore
  initial_apps:
    filebrowser:
      image: portalapps.azurecr.io/ptl-apps/filebrowser:master
      port: 80
      data_dirs:
        - /data
      authentication:
        default_access: private
        public_paths:
          - /share/
          - /static/
          - /api/public/

log:
  portal_core: debug
