path_root: "/"

dns:
  zone: p.getportal.org
  prefix length: 6

services:
  postgres:
    host: postgres
    port: 5432
    user: postgres
    password: ES9BOPusdVKoydq6
  backup:
    included_globs:
      - core/portal_core_config.yml
      - core/portal_core_db.json
      - user_data/**/*

apps:
  app_store:
    project_id: 25524454
  lifecycle:
    refresh_interval: 10
  initial_apps:
    - filebrowser

management:
  # api_url: foo

log:
  levels:
    portal_core: info
    gunicorn: warning
